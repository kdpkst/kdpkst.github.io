enlarge and beautify the font in navbar
low performance when displaying the background images. compress the images first (use a default image for each page, but add a left arrow at page left, right arrow at page right. when user clicks, change background image)
split css files

modular improvement: start from About Me